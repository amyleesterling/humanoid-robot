# Firmware release area

Firmware will be separated into `watchdog/` and `supervisor/`. Safety behavior requires hardware-in-the-loop tests. Example motion code is not permitted to energize the assembled arm until T0–T3 gates are complete.

