# Sol R12 disposition after R124

Status: **PRELIMINARY - NOT APPROVED FOR FABRICATION, MOTION OR ENERGIZATION**

Sol's supplied independent engineering verdict remains review round R12. It is not counted again.

R124 makes two narrow corrections relevant to Sol's stopping evidence finding:

- it removes an obsolete `15..125 deg` J2 position row from the active control narrative and restores consistency with the current fail-closed `15..115 deg` firmware/mechanical binding; and
- it makes the 3-degree J2-positive stopping approach explicit, including the result that the ordinary 300 ms heartbeat detection leaves no downstream travel margin at 10 degrees/s and exceeds the approach at 30 degrees/s.

This does **not** close Sol's stopping, safety or buildability findings. Total stopping time is unmeasured; the credited safety-function response limit and PLr/SIL are unallocated; the contactor application is not approved; rail decay and residual travel are unmeasured; J1 minimum/maximum and J2 minimum stops are undesigned; guard reconciliation and qualified validation are absent. All sixteen R124 test cases remain `NOT EXECUTED` and `NOT AUTHORIZED`, and `EG-026` remains open.
