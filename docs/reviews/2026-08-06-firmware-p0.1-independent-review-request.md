# Project Button HR-V0 Firmware P0.1 Independent Review Request

Review date: 2026-08-07
Controlled candidate: **HR-V0-FW-P0.1 / HR-V0-WD-BUILD-P0.1**
Electrical dependency: **Electrical V3-P1.2 / PCB-P0.5 candidate**
Status: **PRELIMINARY—NOT APPROVED FOR FABRICATION OR ENERGIZATION**

## Objective

Independently review the watchdog target/source and supervisor source models for correctness, completeness, fail-closed behavior and agreement with `SAFE-003`, `CTRL-003`, Electrical V3-P1.2 / PCB-P0.5 and the energization gates. Reproducible binaries are not target-execution evidence, HIL evidence, functional-safety validation or permission to flash or energize.

## Controlled inputs

- `firmware/watchdog/`: portable C state logic, exact Pico binding, JSON configuration, executable Python reference model, unit tests and controlled P0.1 artifacts;
- `firmware/watchdog/toolchain-lock.json` and `firmware/watchdog/output/P0.1/`: publisher-pinned tools, two-build evidence, executable artifacts, size and static stack records;
- `firmware/supervisor/`: Python authority/state model, JSON configuration and unit tests;
- `firmware/supervisor/actuator-config.json` and `project_button_supervisor/actuator_config.py`: fail-closed DYNAMIXEL readback candidate;
- `firmware/SOURCE-MANIFEST.csv`: controlled source hashes;
- `tools/build_hr_v0_watchdog.ps1` and `tools/check_hr_v0_firmware.py`: two-build reproducer and source/artifact checker;
- `docs/hr-v0-firmware-p0.1.md`: design claims, limits and open implementation work;
- `docs/hr-v0-watchdog-build-p0.1.md`: platform/build claims, exact tool sources and verification boundary;
- `docs/hr-v0-actuator-current-envelope-p0.1.md` and `tests/forms/hr-v0-actuator-current-characterization-template.csv`;
- `docs/control.md`, `docs/safety-functions.md`, `requirements/requirements.csv`; and
- `electrical/kicad/project-button-v3/`: electrical dependency to be reviewed with the separate V3-P1.0 / PCB-P0.1 brief.

## Reproduce

```text
python tools/check_hr_v0_firmware.py
python tools/check_traceability.py
python tools/check_energization_gates.py --through-stage E2 --require-ready
```

On Windows AMD64, download and verify the exact archives in `firmware/watchdog/toolchain-lock.json`, extract them under one tool root, then run:

```text
powershell -ExecutionPolicy Bypass -File tools/build_hr_v0_watchdog.ps1 -ToolRoot <PINNED_TOOL_ROOT>
```

The gate command is expected to remain nonzero. Do not weaken gates to obtain a passing readiness result.

## Required review

1. Inspect every source, test, configuration, toolchain-lock, artifact and manifest row; independently reproduce all 25 tests and the two clean target builds.
2. Verify outputs default off, three valid heartbeat edges are required before KWD commands, stale/too-fast/invalid feedback faults drop both channels, and diagnostic faults cannot be cleared online.
3. Examine boundary timing at exactly 300 ms, timer wrap/overflow, clock monotonicity, scheduler latency, relay pickup/dropout, bounce and feedback-settle assumptions. Treat the 20 ms and 25 ms values as preliminary configuration, not released hardware ratings.
4. Prove that boot, reboot, heartbeat restoration, RESET, ARM, configuration load, network recovery, replayed commands and fault acknowledgement cannot create torque or motion.
5. Verify that nominal watchdog loss requires a later physical SR1 RESET and distinct physical SRA1 ARM, and that a fresh validated trajectory after those actions is still required before torque enable.
6. Audit configuration and kinematic hash checks, session binding, command sequence/time checks, start-pose tolerance, joint/TCP/gripper limits, target invalidation and deep-copy/ownership behavior.
7. Identify every mismatch between Python reference behavior and portable C behavior, every unspecified interface, and every condition not covered by tests.
8. Perform FMEA for stuck heartbeat, stuck driver, welded KWD contact, false feedback, shared power/clock/controller/firmware faults, corrupted configuration, process restart and lost communications. Do not assign PL/SIL credit to the ordinary controller or relays.
9. Verify the pinned tool downloads against current primary publishers, reproduce every controlled hash, inspect the ELF/map/disassembly, independently confirm low-before-output GPIO startup, 100 ms hardware-watchdog configuration, 1 ms loop, memory layout and stack evidence, and identify the static-analysis, coverage and disconnected-load HIL evidence still needed.
10. Independently audit the `HR-V0-ACT-P0.1` register sequence, mode-change resets, torque-off EEPROM rule, startup/drive bits, identity/firmware checks, raw current calculations and external characterization plan. Do not equate internal DYNAMIXEL current with branch supply current or treat the raw 800/300 candidates as released limits.

## Deliverable

Return BLOCKER / MAJOR / MINOR findings with exact file, function, line or requirement references; a corrected patch for any proposed source change; independently reproduced test/build output and hashes; and separate verdicts for source-model review, target compilation, target execution, disconnected-load HIL, control-only energization and actuator energization.

Do not mark the design approved or authorize energization.
