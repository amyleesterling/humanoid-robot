# Project Button HR-V0 Firmware P0.2 Independent Review Request

Review date: 2026-08-07

Controlled candidate: **HR-V0-FW-P0.1 / HR-V0-WD-BUILD-P0.2 / HR-V0-WD-HOST-VECTOR-P0.1**

Electrical dependency: **Electrical V3-P1.2 / PCB-P0.5 candidate**

Status: **PRELIMINARY—NOT APPROVED FOR FABRICATION OR ENERGIZATION**

## Objective

Independently review the watchdog target/source and supervisor source models for correctness, completeness, fail-closed behavior and agreement with `SAFE-003`, `CTRL-003`, Electrical V3-P1.2 / PCB-P0.5 and the energization gates. Reproducible binaries and host execution are not RP2040 execution evidence, HIL evidence, functional-safety validation or permission to flash or energize.

## Controlled inputs

- `firmware/watchdog/`: portable C state logic, exact Pico binding, JSON configuration, Python reference model, unit/differential tests and controlled artifacts;
- `firmware/watchdog/toolchain-lock.json` and `firmware/watchdog/output/P0.2/`: publisher-pinned target tools, two-build evidence, executable artifacts, size and static stack records;
- `firmware/watchdog/host-test-toolchain-lock.json` and `firmware/watchdog/output/host-vector/P0.1/`: publisher-pinned host compiler, reproducible compiled runner and differential-execution evidence;
- `firmware/supervisor/`: Python authority/state model, JSON configuration and unit tests;
- `firmware/supervisor/actuator-config.json` and `project_button_supervisor/actuator_config.py`: fail-closed DYNAMIXEL readback candidate;
- `firmware/SOURCE-MANIFEST.csv`: controlled source hashes;
- `tools/build_hr_v0_watchdog.ps1`, `tools/build_hr_v0_watchdog_host_runner.ps1` and `tools/check_hr_v0_firmware.py`;
- `docs/hr-v0-firmware-p0.1.md`, `docs/hr-v0-watchdog-build-p0.2.md` and `docs/hr-v0-actuator-current-envelope-p0.1.md`;
- `docs/control.md`, `docs/safety-functions.md`, `requirements/requirements.csv`; and
- `electrical/kicad/project-button-v3/`: electrical dependency reviewed under its separate brief.

## Reproduce

```text
python tools/check_hr_v0_firmware.py
python tools/check_traceability.py
python tools/check_energization_gates.py --through-stage E2 --require-ready
```

On Windows AMD64, verify and extract the exact archives in both watchdog toolchain locks, then run:

```text
powershell -ExecutionPolicy Bypass -File tools/build_hr_v0_watchdog.ps1 -ToolRoot <PINNED_PICO_TOOL_ROOT>
powershell -ExecutionPolicy Bypass -File tools/build_hr_v0_watchdog_host_runner.ps1 -ToolRoot <PINNED_LLVM_MINGW_ROOT>
```

The gate command is expected to remain nonzero. Do not weaken gates to obtain a passing readiness result.

## Required review

1. Inspect every source, test, configuration, lock, artifact and manifest row; independently reproduce all 29 tests, both clean target builds and both clean host builds.
2. Verify outputs default off, three valid heartbeat edges are required before KWD commands, and stale/too-fast/invalid-feedback/clock-regression faults drop both channels.
3. Independently analyze the unsigned half-range rule, exact 19/20 ms, 24/25 ms and 299/300 ms boundaries, legitimate clock wrap, regression and intervals of `2^31` ms or more.
4. Compare compiled C with the Python model beyond the controlled 44 steps. Identify missing vectors, undefined behavior, integer-conversion defects, compiler dependence and coverage gaps.
5. Prove that boot, reboot, heartbeat restoration, RESET, ARM, configuration load, network recovery, replayed commands and fault acknowledgement cannot create torque or motion.
6. Verify that nominal watchdog loss requires a later physical SR1 RESET and distinct physical SRA1 ARM, plus a fresh validated trajectory before torque enable.
7. Audit configuration/kinematic hashes, session and command checks, start-pose tolerance, joint/TCP/gripper limits, target invalidation and ownership behavior.
8. Perform FMEA for stuck heartbeat/driver/contact/feedback plus shared power, clock, controller, firmware, configuration, restart and communication faults. Assign no PL/SIL credit.
9. Reproduce every controlled hash; inspect ELF/map/disassembly, GPIO startup, processor watchdog, loop, memory and stack evidence; identify static-analysis, coverage and target-HIL work still needed.
10. Audit the `HR-V0-ACT-P0.1` register sequence and external characterization plan. Do not equate internal actuator current with branch supply current or release raw 800/300 candidates.

## Deliverable

Return BLOCKER / MAJOR / MINOR findings with exact file, function, line or requirement references; a corrected patch for any proposed source change; independently reproduced test/build output and hashes; and separate verdicts for source-model review, host execution, target compilation, target execution, disconnected-load HIL, control-only energization and actuator energization.

Do not mark the design approved or authorize energization.
